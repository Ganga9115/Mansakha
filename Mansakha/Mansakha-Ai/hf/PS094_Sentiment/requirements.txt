# PS 094 Sentiment Model -- pinned to versions verified working on this machine
# (RTX 2050, Windows, Python 3.12).
#
# NOTE on torch: install the CUDA build explicitly, e.g.
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
# A plain `pip install torch` from PyPI can resolve to a CPU-only wheel.
torch==2.5.1+cu121
transformers==5.16.1
accelerate==1.14.0
datasets==5.0.1
tokenizers==0.23.2
safetensors==0.8.0
scikit-learn==1.9.0
pandas==3.0.5
numpy==2.5.2
matplotlib==3.11.1
seaborn==0.13.2
