
Role: Backend Engineer with 30+ years of experience in Node.js, Express, and PostgreSQL/Supabase, specializing in secure, government-grade, multi-tenant systems with strict role-based access control and PII isolation.

Task: Implement every feature listed below, one at a time, in the existing Mansakha backend (`Mansakha/backend/`, Node/Express/Supabase). This is not a redesign — extend the existing codebase so every feature in the Mansakha Feature Catalog has a real, working endpoint behind it. Do not skip, merge, or summarize features; each row is its own deliverable. Reuse everything already built: `verifyToken`/`requireRole`/`requireJurisdiction` middleware, the `{success, data, message}` envelope (`ok()`/`fail()`), rate-limit middleware, `audit_log` writes, and the AI pipeline (`services/gemini.js`, `services/scoring.js`, `POST /api/ai/analyze-interaction`). This document walks the feature catalog role by role, subsection by subsection, feature by feature — for each one it says exactly what already exists (reuse it) or exactly what to build (schema + endpoint + logic).

---

## 1. VICTIM

### 1.1 Login

| Feature                                  | Spec                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Docket ID + Full Name + State + District | **New.** `POST /api/auth/victim/login` — body `{docketNumber, fullName, stateName, jurisdictionId}`. Query: `victims` joined to `victim_identity` (for `full_name`) and `jurisdictions` (for `state`/`district` name resolution — a district's `parent_id` must resolve to the given state). Case-insensitive exact match on all four. Match → `signToken({type:'victim', victimId})`, same shape as today. No match → generic `fail(res, 'No matching record found', 401)` (don't reveal which field was wrong). Rate-limit identically to `staffLoginLimiter` (10/15min/IP) — this is now a credential-guessing surface. **This replaces** `POST /api/auth/victim/otp/request`, `/otp/verify`, `/phone-otp/*`, `/google`, and self-registration `/register` — remove them and their supporting services (`otpService.js`, `twilioVerify.js`, Google OAuth client code, `email_otp_codes` table) rather than leaving them dead. |
| Auto-detect State & District (GPS)       | **New.** `POST /api/victim/gps-lookup` — body `{lat, lng}`. Server-side call to Nominatim's reverse-geocode endpoint (`https://nominatim.openstreetmap.org/reverse`), extract state/district-equivalent fields from the response, match against `jurisdictions` by name (fuzzy/`ILIKE` match, since OSM naming won't always exactly match your seeded jurisdiction names — log a warning and return null gracefully on no match, don't 500). Return `{stateName, jurisdictionId}`. No auth required (called pre-login), but rate-limit it (e.g. 20/hour/IP) since it's an unauthenticated public route hitting a third-party API on your behalf.                                                                                                                                                                                                                                                                                                                       |
| State dropdown                           | **Existing, reuse.** `GET /api/lookups/jurisdictions?level=state` (confirm exact query param name in `lookups.js`; add the level filter if it isn't already there).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| District dropdown                        | **Existing, reuse.** Same endpoint filtered by `level=district&parentId=<stateId>` — confirm `lookups.js` supports the `parentId` filter; add it if not.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Change password                          | **New** (victims didn't have passwords in the OTP-based model unless they opted into one). Decide: does docket-based login need a password at all, or does knowing all 4 fields *is* the credential? If a password layer is wanted on top, add `victims.password_hash` (may already exist per earlier session work — confirm) and `POST /api/auth/victim/change-password`, mirroring `auth.staff.js`'s existing route exactly. **If no password concept survives the login-model change, remove this from scope rather than building it against nothing.**                                                                                                                                                                                                                                                                                                                                                                                                            |

### 1.2 Dashboard

All of these are fields on the existing `GET /api/victim/dashboard` — confirm/extend the response shape rather than adding new routes:

| Feature                          | Spec                                                                        |
| -------------------------------- | --------------------------------------------------------------------------- |
| Case status                      | **Existing** — `caseStatus.status`/`caseStage` already returned. |
| Current distress level           | **Existing** — `currentDistressLevel`.                             |
| Upcoming check-in date           | **Existing** — `nextCheckIn`.                                      |
| Alerts & important notifications | **Existing** — `alerts[]`.                                         |
| Available supports               | **Existing** — `supportLinks[]`.                                   |

### 1.3 Check-in & Interaction

| Feature                                | Spec                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Mental health check-in (questionnaire) | **Existing.** `POST /api/victim/checkin` — unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| AI Chat                                | **New.** `POST /api/victim/chat` — body `{message, conversationId?}`. Calls `gemini.js` with a conversational system prompt (distinct from the structured sentiment-extraction prompt used by `/checkin` — add a second prompt template, don't reuse the check-in one as-is). Decide explicitly whether each chat turn also writes an `interactions`/`distress_scores` row (continuous passive monitoring) or is purely conversational with no scoring side-effect — write that decision as a comment at the top of the route handler. Store the conversation (`chat_messages: message_id, victim_id, sender, body, sent_at`) so history persists across sessions. |
| IVRS Call                              | **New.** `POST /api/victim/ivrs/trigger` — inserts a `dispatch_queue` row of type `ivrs_call`. The dispatch worker (`dispatchWorker.js`) drains it and calls Exotel's API if configured; if Exotel isn't configured, log `"IVRS provider not configured"` and leave the queue entry in a `failed`/`pending` state — **never return a fabricated "call placed" success to the client** if no real call was attempted.                                                                                                                                                                                                                                          |
| SMS check-in                           | **New.** A `dispatch_queue` entry type `sms_checkin_prompt`, drained by the existing worker, sent via whatever SMS provider is actually configured (Twilio is already a dependency for OTP today — if OTP is removed per §1.1, confirm whether Twilio stays for this purpose or gets removed too). Inbound SMS responses (the victim replying to the prompt) need a webhook receiver (`POST /api/webhooks/sms-inbound`) if you want two-way SMS check-in, not just outbound prompts — scope this explicitly, it's a meaningfully bigger feature than outbound-only.                                                                                                        |
| Multilingual interaction               | **Existing schema, needs wiring.** The `languages` table and lookup endpoint already exist (see Ministry §6.4) — this row is really about the frontend actually calling it (see frontend prompt), not new backend work, beyond confirming `GET /api/lookups/languages` returns all 6 seeded languages correctly.                                                                                                                                                                                                                                                                                                                                                              |

### 1.4 Counsellor Preference

| Feature                              | Spec                                                                                                                                                                                                                                                                                                                         |
| ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Opt-in for Manual Counsellor         | **New.** Add `victims.opted_for_manual_counsellor boolean not null default false`. `PATCH /api/victim/counsellor-preference` — body `{optedIn: boolean}`.                                                                                                                                                       |
| In-app chat with assigned counsellor | **New.** `messages` table: `message_id, victim_id, official_id, sender_type ('victim'                                                                                                                                                                                                                                |
| Call Counsellor button               | **New, minimal.** `GET /api/victim/assigned-counsellor` returns the assigned counsellor's name + contact number (from `officials.phone`) only when opted in and assigned — the frontend renders a `tel:` link from this. No backend call-routing/VoIP infrastructure implied unless explicitly requested later. |

### 1.5 Stress-Level Automated Response

This is one new function, `applyStressResponse(victimId, riskLevel)`, called immediately after every `distress_scores` insert (in `/checkin`, `/chat` if it scores, and anywhere else a score is produced) — put it in a new `services/stressResponse.js`, don't inline it in route handlers:

| Distress Level                       | Spec                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Low                                  | No-op. Existing behavior, unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Moderate (above normal)              | Insert a`dispatch_queue` entry type `wellness_push` with a suggested category (`exercise`/`meditation`/`music`, simple rotation or pick based on the dominant negative-emotion signal from `interaction_signals` — a reasonable default, not over-engineered). Drained as a push notification via the existing worker.                                                                                                                                           |
| High                                 | Insert a`dispatch_queue` entry type `ai_proactive_contact`. When drained: call Gemini to generate a short, context-aware check-in message (reference the victim's recent trend if you want it to feel aware, but keep the prompt simple), deliver via push (or SMS if push token absent).                                                                                                                                                                                 |
| Critical — not opted for counsellor | Query`official_roles` for active Counsellor/Administration officials in the victim's jurisdiction, pick the one with the fewest currently-open assigned cases (a `count(*) where assigned_counsellor_id = official_id and status != 'resolved'`-style query — no real presence tracking needed). Set `victims.assigned_counsellor_id`, insert `alert_notifications` for them with a flag/column marking it `auto_assigned = true` so the UI can show that context. |
| Critical — opted for counsellor     | Insert an`alert_notifications` row for the *already*-assigned counsellor with elevated priority — add `alert_notifications.priority` (`'normal'                                                                                                                                                                                                                                                                                                                        |

### 1.6 Wellness & Self-Care

| Feature                       | Spec                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Exercise suggestions          | **New, simple.** `GET /api/victim/wellness-suggestions?category=exercise` — serve from a small static config/table (`wellness_content: content_id, category, title, body, language_id`), not AI-generated.                                                                                                                                                                                                                                                                                                                             |
| Guided meditation & breathing | **New.** Same `wellness_content` table, `category=meditation`, with a `duration_seconds` and step-by-step `body` (JSON array of steps) the frontend renders as a timer sequence — no backend logic beyond serving the content.                                                                                                                                                                                                                                                                                                     |
| Music suggestions             | **New.** Same table, `category=music`, `body` holds title/artist/external URL — no audio hosting/streaming needed, just links.                                                                                                                                                                                                                                                                                                                                                                                                         |
| Journal writing               | **New.** `journal_entries: entry_id, victim_id, content, language_id, sentiment_score, created_at`. `POST /api/victim/journal` runs `content` through `gemini.js`'s existing sentiment analysis (reuse, don't duplicate), stores the resulting score on the entry — **decide explicitly** whether this also feeds into `distress_scores`/the trend line, or stays a separate signal purely for the victim's own reflection; comment the decision. `GET /api/victim/journal` lists past entries, newest first, paginated. |

### 1.7 Distress History

**Existing, unchanged.** `GET /api/victim/distress-history` already covers both rows (score visibility, trend data for the chart).

### 1.8 Settings

| Feature                         | Spec                                                                                                                                                                     |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Preferred language              | **Existing.** `PATCH /api/victim/language`.                                                                                                                      |
| Profile                         | **Existing.** Covered by `GET /api/victim/dashboard`'s existing fields; add `fullName`/`docketNumber` if not already present now that login is docket-based. |
| Manual counsellor opt-in toggle | See §1.4.                                                                                                                                                               |
| Consent management              | **Existing.** `consent_records` + `GET/POST /api/victim/consent`, `/consent-status` already real — no change needed.                                        |
| Change password                 | See §1.1's note — only build if the login-model decision keeps a password concept.                                                                                     |

---

## 2. COUNSELLOR

### 2.1 Login — **Existing, unchanged.** `POST /api/auth/staff/login` (roleName: `Counsellor`) + `POST /api/auth/staff/change-password`, both already real.

### 2.2 Dashboard

| Feature                                    | Spec                                                                                                                                                                                                                                                                                     |
| ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Personalised login view                    | **Existing.** `GET /api/counsellor/cases` already scopes to `assigned_counsellor_id = req.auth.officialId` (confirm this exact filter is in place now that assignment is load-bearing per §1.5, not just a workload stat).                                                    |
| List of assigned victims with back-stories | **Existing endpoint, extend response.** Add a `caseBackground`/`backstory` field to `GET /api/counsellor/cases` — check whether `victims`/`victim_identity` already has a free-text field for this (e.g. `address` or notes) or add `victims.case_background text`. |
| Segregated by stress level (AI-ranked)     | **Existing.** The priority-sort logic (`RISK_SORT_ORDER`) in `counsellor.js` already does this.                                                                                                                                                                                |
| Total case count                           | **Existing.** `GET /api/counsellor/dashboard`'s existing counts.                                                                                                                                                                                                                 |
| Scheduled counsellings                     | **New.** `counselling_sessions: session_id, victim_id, counsellor_id, scheduled_at, status ('upcoming'                                                                                                                                                                             |
| Alert system — SOS trigger                | See §1's new`sos_events` — extend `GET /api/counsellor/alerts` to include SOS-sourced entries, tagged with a `source: 'sos'` field (add `alerts.source` or `alert_notifications.source` column, `'distress_score'                                                            |

### 2.3 Case Management

| Feature                              | Spec                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Victim case detail view              | **Existing.** `GET /api/counsellor/cases/:victimId`.                                                                                                                                                                                                                                                                                                                                                                                                          |
| Personalised victim records          | **Existing**, extended by the backstory field above.                                                                                                                                                                                                                                                                                                                                                                                                            |
| AI chart values shown to counsellor  | **Existing.** Already returns `interaction_signals`/trend data consumed by the frontend's chart.                                                                                                                                                                                                                                                                                                                                                              |
| Explainable risk report              | **Existing.** `distress_scores.explanation` (Gemini's reasoning) already stored and returned.                                                                                                                                                                                                                                                                                                                                                                 |
| Intervention recommended             | **Existing.** `distress_scores.suggested_intervention_type_id` already computed and returned.                                                                                                                                                                                                                                                                                                                                                                 |
| Case notes — AI / Manual            | **Existing.** `case_notes` table + `GET/POST /api/counsellor/cases/:victimId/notes` already real (confirm AI auto-draft-after-interaction is actually wired, not just manual-entry-only — if AI drafting isn't happening yet, add it: after each check-in, call Gemini once more with a "summarize this interaction as a case note" prompt and insert a `case_notes` row authored as `'ai'`, editable by the counsellor before it's considered final). |
| Decision making panel                | **No new backend** — this is `intervention_types` (already seeded with all 7 PS-named types) presented as labeled buttons on the frontend; no schema/endpoint change needed beyond what Log Intervention already uses.                                                                                                                                                                                                                                       |
| Log intervention                     | **Existing.** `POST /api/counsellor/cases/:victimId/intervention`.                                                                                                                                                                                                                                                                                                                                                                                            |
| Mark intervention complete           | **Existing** — confirm `PATCH /api/counsellor/intervention/:id/complete` sets `completed_at` and updates whatever case-status field reflects it.                                                                                                                                                                                                                                                                                                           |
| In-app chat with victim              | See §1.4's`messages` table — the counsellor-side mirror: `GET/POST /api/counsellor/cases/:victimId/messages`, same opt-in gating.                                                                                                                                                                                                                                                                                                                               |
| Ping to call victim (Critical alert) | Covered by §1.5's automated response — the "urgent ping"*is* this feature from the counsellor's receiving end; no separate endpoint, just make sure the urgent `alert_notifications` row is queryable via the existing alerts endpoint with its priority flag visible.                                                                                                                                                                                          |
| AI follow-up tracking                | **Existing** — `interventions.completed_at` presence/absence already gives you this; confirm it's surfaced on Case Detail's response, add it if missing.                                                                                                                                                                                                                                                                                                     |

### 2.4 Alerts

| Feature              | Spec                                                                                                                                                                                                                                                                                                                                                                                      |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Real-time alert feed | **Existing endpoint, not actually real-time** — `GET /api/counsellor/alerts` works but is fetch-once on the client. Backend-side, enabling genuine real-time means confirming the Supabase Realtime publication (already present, `security_and_realtime.sql`) is actually usable — no backend change needed here, this is a frontend subscription gap (see frontend prompt). |
| SOS alert            | See §1.5/§2.2 —`sos_events` + the `source` flag.                                                                                                                                                                                                                                                                                                                                   |

---

## 3. DISTRICT ADMINISTRATION

### 3.1 Login — **Existing, unchanged.** Same `auth.staff.js` route, `roleName: 'Administration'`, jurisdiction level resolved via `official_roles` as already built this session.

### 3.2 Victim Access Scope — **Existing.** `requireJurisdiction('district')` already enforces this on `admin.js` routes.

### 3.3 Dashboard

All six rows (**Total cases in district, Vulnerable victims, High-risk cases, Critical cases, Alerts feed, Distress trends**) are **existing** — `GET /api/admin/dashboard/:jurisdictionId`'s district-tier response already returns case-level data covering all of these per the PS Alignment audit. Confirm each named metric is explicitly present in the response shape (add any missing one as a computed field, not a new endpoint).

### 3.4 Case Access

| Feature                      | Spec                                                                                                                                                                                                                                                                                       |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Case detail view (read-only) | **Existing** — same `GET /api/counsellor/cases/:victimId`-equivalent shape should be reachable for Administration too; confirm `admin.js` has its own read-only case-detail route or reuses the counsellor one with a role check that strips/hides intervention-logging fields. |
| Counsellor workload view     | **New if not already present** — `GET /api/admin/workload/:jurisdictionId`: per-counsellor case count and average response time (`avg(resolved_at - triggered_at)` over that counsellor's `alerts`).                                                                          |

### 3.5 Victim Credential Management

| Feature                   | Spec                                                                                                                                                                                                                                                                                                                                                |
| ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Create victim credentials | **New.** `POST /api/admin/victims` — `{docketNumber, fullName, stateName, jurisdictionId, caseTypeId, caseStage}`, `requireJurisdiction('district')` so a District Admin can only create victims inside their own district. Inserts `victims` + `victim_identity` rows — this is the write-side counterpart to §1.1's new login. |
| Edit victim record        | **New.** `PATCH /api/admin/victims/:victimId` — case stage / contact detail updates, same jurisdiction guard.                                                                                                                                                                                                                              |

### 3.6 Reporting

| Feature                  | Spec                                                                                                                                                                                                                                                                                                                          |
| ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Generate & share reports | **New.** `reports: report_id, jurisdiction_id, generated_by, generated_at, period_start, period_end, snapshot jsonb`. `POST /api/admin/reports/generate` snapshots the current dashboard numbers (§3.3's metrics) into a `reports` row — this is what makes "share upwards with Ministry" concrete (see §6.5). |

---

## 4. STATE/UT ADMINISTRATION

### 4.1 Login — **Existing**, same as District, jurisdiction level `state`.

### 4.2 Victim Access Scope — **Existing**, `requireJurisdiction('state')`.

### 4.3 Dashboard

All four rows (**State/UT-level monitoring, District-wise breakdown, Rising-trend districts, Drill-down to district → victim**) are **existing** — the state-tier aggregate response in `GET /api/admin/dashboard/:jurisdictionId` already returns per-district rollups per the PS Alignment audit. "Rising-trend districts" specifically may need a small addition: a `trendDirection` field per district row (compare this period's average score to last period's) if it isn't already computed — add it as a derived field, not a new table.

### 4.4 Reporting — **Same as §3.6**, `POST /api/admin/reports/generate` reused with the caller's own (state-level) jurisdiction.

---

## 5. NATIONAL ADMINISTRATION

### 5.1 Login — **Existing**, jurisdiction level `national`.

### 5.2 Dashboard

All four rows (**National-level monitoring, State-wise breakdown, Drill-down: State → District → Victim, Policy-input trend lines**) are **existing** at the national tier of `GET /api/admin/dashboard/:jurisdictionId` per the audit. "Policy-input trend lines" — confirm the response includes a national-level time series (not just current-snapshot counts); add a `GET /api/admin/dashboard/:jurisdictionId/trend?months=6`-style endpoint if a longitudinal view isn't already returned.

---

## 6. SUPER ADMIN (MINISTRY)

### 6.1 Login — **Existing.** `POST /api/auth/ministry/login`, fully real.

### 6.2 Role & Account Management

| Feature                          | Spec                                                                                                                                                                                                                                                                                                                                        |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Create Counsellor accounts       | **Existing.** `POST /api/ministry/staff` with `roleName: 'Counsellor'`.                                                                                                                                                                                                                                                           |
| Create District Admin accounts   | **Existing**, `roleName: 'Administration'`, jurisdiction level `district`. **New validation to add:** enforce the "Limit: 1 per District" rule — check for an existing active `official_roles` row at that exact `jurisdiction_id` + role before allowing a second one; return a clear `fail()` message if violated. |
| Create State Admin accounts      | **Existing** creation path; **same new limit check**, 1 per State/UT.                                                                                                                                                                                                                                                           |
| Create National Admin accounts   | **Existing** — no stated limit in the catalog, leave unlimited unless told otherwise.                                                                                                                                                                                                                                                |
| Revoke / deactivate accounts     | **Existing.** `PATCH /api/ministry/staff/:id/revoke` (soft-revoke via `official_roles.revoked_at`).                                                                                                                                                                                                                               |
| Role-Based Access Control (RBAC) | **Existing**, enforced throughout via `requireRole`/`requireJurisdiction` — no new work, this row is describing existing architecture.                                                                                                                                                                                           |
| PII Isolation enforcement        | **Existing** — `victim_identity` separation + deny-all RLS already in place per the audit.                                                                                                                                                                                                                                         |

### 6.3 Dashboards & Oversight

| Feature                             | Spec                                                                                                                                                                                                                                                                                                     |
| ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Role-scoped aggregate dashboards    | **Existing** — Ministry can call the same `admin.js` dashboard endpoints unrestricted (no `requireJurisdiction` limit applies to Ministry, per the original Build Prompt's Section 3).                                                                                                        |
| Heatmaps by state / region          | **New.** `GET /api/ministry/heatmap` — `group by jurisdiction (state level)`, `count(victims)`, `avg(latest distress_scores.score_value)`. Pure aggregation, no new table.                                                                                                                |
| Connections with NHAA helpline IVRS | **New, simulated** — see §1.3's IVRS dispatch queue; this row is about Ministry having *visibility* into IVRS activity (a simple `GET /api/ministry/ivrs-log` listing `dispatch_queue` entries of type `ivrs_call` and their status), not a new integration.                             |
| Tele-MANAS integration              | **Out of scope for this pass** unless a real Tele-MANAS API/contact exists to integrate with — don't build a fake integration; if this needs to exist for the demo, make it an explicit static "escalation contact" reference (a phone number/link), not a simulated API call dressed up as real. |
| Inter-department coordination       | **No new backend** — this reads as a description of the overall system (officials + audit_log already link welfare/counselling activity), not a discrete feature to build.                                                                                                                        |
| Receive Base-Level Reports          | **New.** `GET /api/ministry/reports?jurisdictionLevel=&status=` — lists `reports` rows from §3.6/§4.4, paginated.                                                                                                                                                                           |
| Audit log viewer                    | **Existing.** `GET /api/ministry/audit-log`, already paginated and real.                                                                                                                                                                                                                         |

### 6.4 System Configuration

| Feature                    | Spec                                                                                                                                                                                  |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Manage case types          | **New.** `GET/POST/PATCH/DELETE /api/ministry/case-types` — mirror the existing `languages` CRUD exactly (soft-delete via `deleted_at`, same validation/response shape). |
| Manage intervention types  | **New.** Same pattern, `/api/ministry/intervention-types`.                                                                                                                    |
| Manage channels            | **New.** Same pattern, `/api/ministry/channels`.                                                                                                                              |
| Manage supported languages | **Existing.** `/api/ministry/languages` already fully real — this is the template the three rows above copy.                                                                 |

---

## 7. DATA INTAKE & INTEGRATION ADMIN

This role does not exist today — add it fully:

- **Schema:** either a 4th `roles` row (`'Data Intake Admin'`) or model it as a scoped `Administration` variant — pick the dedicated-role approach for clarity, since its permission set doesn't overlap with District/State/National Administration at all.
- **`routes/dataIntake.js`** (new file, registered in `server.js` as `/api/data-intake`).

| Feature                             | Spec                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Admin ID + Email + Password (login) | **New**, but reuses `auth.staff.js`'s existing login logic — just add `'Data Intake Admin'` to the allowed `roleName` list in that route's validation.                                                                                                                                                                                                                                                                                                                                       |
| Change password on first access     | **Existing mechanism, new applicability** — `must_change_password` flag already exists on `officials`; no new code, just confirm it's set `true` when Ministry provisions a Data Intake Admin account.                                                                                                                                                                                                                                                                                       |
| Create victim credentials           | **New**, but identical logic to §3.5's District Admin version — extract the insert logic into a shared `services/victimProvisioning.js` function both routes call, rather than duplicating the insert code. Data Intake Admin is *not* jurisdiction-limited the way District Admin is (per the catalog's framing as an integration/intake role, not a district-operational one) — confirm this is the intended scope before removing the jurisdiction guard.                                 |
| Fetch case details                  | **New, explicitly simulated.** `POST /api/data-intake/fetch-case` — body `{docketNumber}`, returns plausible-shaped fixture data (name/case-type/jurisdiction guesses) either from a small local JSON fixture or a clearly-labeled mock response. Add a code comment stating this is a placeholder for a real NHAA/Integrated Portal API integration that doesn't exist yet — don't let this read as a working government API connection in code or in any response text shown to a reviewer. |

---

## 8. AI RISK ENGINE (backend, no UI)

| Feature                           | Spec                                                                                                                                                             |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AI response analysis              | **Existing.** `services/gemini.js`, real Gemini 2.5-flash calls.                                                                                         |
| Dynamic Distress Score Engine     | **Existing.** `services/scoring.js`'s weighted formula (see the PS Alignment report for the disclosed weight-redistribution detail).                     |
| Distress history & trend tracking | **Existing.** `distress_scores` rows queried over time.                                                                                                  |
| Escalation prediction             | **Existing.** `isEscalatingTrend()` — 3-score, ≥10-point monotonic rise check.                                                                         |
| Risk level classification         | **Existing.** `classifyRiskLevel()` thresholds, unit-tested.                                                                                             |
| Automated case prioritisation     | **Existing.** `counsellor.js`'s risk-tier sort on the case queue.                                                                                        |
| Voice stress analytics            | **Explicitly deferred** — do not build in this pass; `voice_stress_score` stays hardcoded 0 per the original Build Prompt's phased plan.                |
| Alert creation & routing          | **Existing**, extend per §1.5/§2.4 for the `source`/`priority` fields.                                                                               |
| Dispatch worker                   | **Existing**, extend with the new queue-entry types from §1.3/§1.5 (`ivrs_call`, `sms_checkin_prompt`, `wellness_push`, `ai_proactive_contact`). |
| Stress-triggered wellness push    | See §1.5 — new logic in`stressResponse.js`.                                                                                                                  |
| High-score AI call trigger        | See §1.5 — new logic in`stressResponse.js`.                                                                                                                  |
| Critical auto-assignment          | See §1.5 — new logic in`stressResponse.js`.                                                                                                                  |
| Critical counsellor ping          | See §1.5 — new logic in`stressResponse.js`.                                                                                                                  |

---

## Suggested build order

1. §1.1 Victim login model change — blocks everything downstream of "a victim exists and can log in."
2. §3.5 District Admin victim creation — needed to have any victims to log in as, post-§1.1.
3. §1.5 / §8 Stress-Level Automated Response engine — the core new cross-cutting logic.
4. §1.4, §1.6, §2.3 (chat/messages, wellness, journal, SOS) — mostly independent, parallelizable.
5. §7 Data Intake Admin — net-new, no dependencies on the above.
6. §6.4 Ministry System Config completion (case types / intervention types / channels) — small, isolated, closes a concrete PS-alignment gap.
7. §3.6 / §4.4 / §6.3 Reports + heatmap — no dependencies, slot in anytime.
